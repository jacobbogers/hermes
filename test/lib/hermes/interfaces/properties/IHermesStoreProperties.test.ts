import { expect } from 'chai';

import { IHermesStoreProperties } from '~lib/hermes/interfaces/properties/IHermesStoreProperties';

describe('~lib/hermes/interfaces/properties/IHermesStoreProperties', () => {
    it('');
});
