import { expect } from 'chai';

import { IStateTransition } from '~lib/adaptors/interfaces/states/IStateTransition';

describe('~lib/adaptors/interfaces/states/IStateTransition', () => {
    it('');
});
